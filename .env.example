# PostgreSQL (docker-compose)
POSTGRES_USER=zerok
POSTGRES_PASSWORD=change-me
POSTGRES_DB=zerok_billing
POSTGRES_PORT=5432

# Backend
SESSION_SECRET=change-me-in-production-min-32-chars
FRONTEND_ORIGIN=http://localhost
# En prod HTTPS : COOKIE_SECURE=true. En local (http) : false ou ne pas définir
COOKIE_SECURE=false

# Port exposé backend sur l'hôte (3011 par défaut pour éviter conflit avec Umami/codeurbase sur 3001)
BACKEND_PORT=3011
